# 目录结构

```
project-root/
├── packages/                    # Monorepo 结构 (推荐) 或清晰的源码目录
│   ├── core/                    # 【核心层】与 UI 无关的纯逻辑
│   │   ├── src/
│   │   │   ├── stores/         # 状态管理 (笔记、分类、用户状态)
│   │   │   ├── services/       # 服务层 (API 客户端、加密服务)
│   │   │   ├── models/         # 数据模型/类型定义 (Note, Category, User)
│   │   │   ├── utils/          # 工具函数 (加密算法、日期处理)
│   │   │   └── index.ts        # 核心库入口
│   │   └── package.json
│   │
│   ├── ui-shared/               # 【共享UI层】基础UI组件库 (无业务逻辑)
│   │   ├── src/
│   │   │   ├── components/     # 纯展示组件 (Button, Modal, Input, Card)
│   │   │   └── styles/         # 基础样式、CSS变量 (定义 --color-primary 等)
│   │   └── package.json
│   │
│   ├── ui-classic/              # 【现有UI层】你当前的、稳定的经典UI
│   │   ├── src/
│   │   │   ├── views/          # 页面组件 (Dashboard.vue, NoteList.vue)
│   │   │   ├── components/     # 业务组件 (使用 ui-shared 和 core)
│   │   │   ├── router/         # 经典UI的路由配置
│   │   │   ├── App.vue         # 经典UI的根组件
│   │   │   └── main.ts         # 经典UI的入口
│   │   ├── index.html
│   │   └── package.json
│   │
│   └── ui-modern/               # 【未来UI层】你将开发的新交互逻辑UI
│       ├── src/
│       │   ├── ... (类似 ui-classic 的结构)
│       └── package.json
│
├── cloudflare/                  # Cloudflare Worker 后端 (保持不变)
│   ├── src/
│   └── wrangler.toml
│
└── package.json (Workspace root)

```
